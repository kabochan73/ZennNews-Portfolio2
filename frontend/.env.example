# Laravel API base URL, used only on the Next.js server (never exposed to the browser).
LARAVEL_API_URL=http://backend:8000

# Shared secret for POST /api/revalidate. Must match the backend's REVALIDATE_SECRET.
REVALIDATE_SECRET=
