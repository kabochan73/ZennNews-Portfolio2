import { HomeEntry } from "@/components/home/HomeEntry";

/** /home: static shell; the browser moves on to the leftmost favorite tag. */
export default function HomePage() {
  return <HomeEntry />;
}
